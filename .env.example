# Copy this file to .env and replace the placeholders. Never commit .env.
#
# The Databricks CLI does not automatically load this file. In bash, use:
#   set -a; source .env; set +a

# Databricks workspace authentication
DATABRICKS_HOST=https://dbc-cb85a885-0f56.cloud.databricks.comDATABRICKS_CONFIG_PROFILE=DEFAULT

# Prefer OAuth authentication via `databricks auth login`. Only uncomment this
# when a personal access token is explicitly required by your workspace.
# DATABRICKS_TOKEN=replace-with-secret

# Development-target bundle defaults. These map to variables in databricks.yml.
BUNDLE_VAR_source_path=/Volumes/workspace/default/gnss_source/BELE00BRA_R_20261050000_01D_30S_MO.parquet
BUNDLE_VAR_station=BELE
BUNDLE_VAR_year=2026
BUNDLE_VAR_doy=105
BUNDLE_VAR_output_table=monitoring.source_profile
                                                                                        